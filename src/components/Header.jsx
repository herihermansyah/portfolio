import React from "react";
import Menu from "../features /navigation/Menu";

const Header = () => {
  return (
    <div id="home" className="bg-header text-white uppercase shadow-xl">
      <Menu />
    </div>
  );
};

export default Header;
